# LCA 年历

2026 / 2027 年度日历页面（年份数字按进度渐变、跨年自动轮转、闰年适配），部署于 GitHub Pages，绑定自定义域名 lca.l.cd。

## 访问

- `http://lca.l.cd/year-calendar/`

## 域名

仓库根目录 `CNAME` 文件内容为 `lca.l.cd`，DNS 处将 `lca.l.cd` 的 CNAME 记录指向 `oooq18.github.io`。
